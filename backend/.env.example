# Allowed CORS origins for API routes (/api/*), comma-separated.
# If not provided, backend defaults to the preview frontend origin for port 3000.
CORS_ALLOWED_ORIGINS=https://vscode-internal-20401-qa.qa01.cloud.kavia.ai:3000
