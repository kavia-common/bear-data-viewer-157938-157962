# Example environment configuration for backend processing and DB persistence
# Copy to .env and adjust values for your environment.

# Flask-related (used by CORS and docs)
CORS_ALLOWED_ORIGINS=http://localhost:3000

# YOLO model selection
YOLO_MODEL_PATH=yolov8n.pt
PROBABILITY_THRESHOLD=0.3

# S3 download configuration (optional; if not set, download is skipped gracefully)
BEAR_S3_BUCKET=identify_bear_bucket
BEAR_S3_KEY=bears.mp4
AWS_REGION=us-east-1
#AWS_ACCESS_KEY_ID=your_key
#AWS_SECRET_ACCESS_KEY=your_secret
#AWS_S3_ENDPOINT_URL=http://minio.local:9000

# Inference loop timing
DOWNLOAD_INTERVAL_SEC=30
INFERENCE_LOOP_SLEEP_SEC=2

# Database URL (used by identify_bears.py)
# Examples:
# SQLite file: sqlite:///bear_data.db
# PostgreSQL: postgresql+psycopg2://user:pass@host:5432/dbname
# MySQL: mysql+pymysql://user:pass@host:3306/dbname
SQLALCHEMY_DATABASE_URI=sqlite:///bear_data.db

# Optional DB pool tuning
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10

# Logging
LOG_LEVEL=INFO
